<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="UpdaterStatusTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary-brush}" />
        <Setter Property="FontSize" Value="{StaticResource AboutVersionSubtitleFontSize-size}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style x:Key="UpdaterProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{StaticResource ThemeBrand-brush}" />
        <Setter Property="Background" Value="{StaticResource ControlBackgroundAlt-brush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="{StaticResource UpdaterProgressBarHeight-size}" />
    </Style>

    <!-- Matches Settings / Info MetadataDialogFooterButtonStyle -->
    <Style x:Key="UpdaterFooterButtonStyle" TargetType="Button">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondary-brush}" />
        <Setter Property="Width" Value="92" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome"
                            Background="{StaticResource ControlBackgroundAlt-brush}"
                            BorderBrush="{StaticResource BorderMuted-brush}"
                            BorderThickness="{StaticResource Border1-thk}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Hover-brush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Pressed-brush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UpdaterPrimaryButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource UpdaterFooterButtonStyle}">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="MinWidth" Value="120" />
    </Style>

    <Style x:Key="UpdaterAutoCloseCountdownStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondary-brush}" />
        <Setter Property="FontSize" Value="{StaticResource SettingsFormAuxFontSize-size}" />
        <Setter Property="Opacity" Value="0.72" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
