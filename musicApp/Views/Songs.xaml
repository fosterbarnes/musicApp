<local:TrackListHostBase x:Class="musicApp.Views.SongsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:musicApp.Views"
             xmlns:views="clr-namespace:musicApp.Views"
             xmlns:converters="clr-namespace:musicApp.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <views:TrackListView x:Name="trackList"
                             ViewName="Songs"
                             SingleClickPlays="False"/>
        <views:EmptyLibraryAddOverlay x:Name="emptyOverlay"
                                      Visibility="{Binding IsLibraryEmpty, RelativeSource={RelativeSource AncestorType=local:SongsView}, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</local:TrackListHostBase>
