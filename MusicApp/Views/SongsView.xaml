<UserControl x:Class="MusicApp.Views.SongsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MusicApp.Views"
             xmlns:converters="clr-namespace:MusicApp.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <views:TrackListView x:Name="trackList"
                             ViewName="Songs"
                             SingleClickPlays="False"
                             PlayTrackRequested="TrackList_PlayTrackRequested" />
        <!-- Empty library overlay: click to open folder picker and scan for music -->
        <Border x:Name="emptyOverlay"
                Background="Transparent"
                Visibility="{Binding IsLibraryEmpty, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}"
                Cursor="Hand"
                MouseLeftButtonDown="EmptyOverlay_MouseLeftButtonDown">
            <materialDesign:PackIcon Kind="MusicNotePlus"
                                     Width="64"
                                     Height="64"
                                     Foreground="#888888"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
