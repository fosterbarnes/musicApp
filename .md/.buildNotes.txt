Main Window:
- Move add music, re-scan, remove music & clear settings to Settings > Library (with options to show in main window)

Updater:
- Create updater exe
- Compares version to github release versions, installs newer version if available
- Automatic install setting
- Launch after install setting
- Add musicApp-updater-swap to facilitate swapping the old updater with the new

Settings:
- Make the title bar dark theme

	General:
	- Populate language dropdown list
	- Determine user's windows install language, show this language first in settings
	Playback:
	- Volume normalization placeholder gui
	- Add code behind for crossfading, update gui
	- Add code behind for changing audio backend

Scripts:
- Stop pre-push scripts form adding extra blank lines to .md files
- Add updater to main bin folder after building
- Add launch flags to run script

Backend/Boring Stuff:
- Replace any existing instances of "MusicApp". Replace them with camelCase "musicApp"
- Add launch flags --settings (--s) and --info(--i) to launch their respective windows
- Stop info/settings windows from blocking usage of main window
- Change selection border color for selected tab items in settings/info windows
- Attempt to improve overall stabilization and memory usage
