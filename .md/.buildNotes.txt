musicApp v0.1.3 frederickRats

Albums:
- Improve load times
- VASTLY improve overall snappyness
- Fix issue where album thumbnails would only start loading when scrolled to and/or visible
- Load entire album list instead of loading in blocks
- Add loading animation
- Attempt to fix issue where we lose our place in the list after performing some actions (not completely fixed)

Info (Right-click > Info):
- Add individual album art downloader

Settings > Library:
- Add a button to scan all songs for missing album artwork
- Re-arrange library management buttons

Backend/Boring Stuff
- Improve song scan speed:
	- Keep track of CPU/RAM usage & overhead
	- Ramp scan speed up/down based on available resources
	- Break scan into chunks, assign a dynamic amount of workers to scan through chunks
	- 42% faster on my local machine

- Remember which tab(view) was open when quitting. Open it on re-launch
- Change default sidebar width to use minimum
- Address minor build warnings
- Fix issue where musicApp would be sent to the back of open windows when closing some dialogs/windows
- If there is an existing fruit related software artwork cache, scan it and embed artwork to each song
	
Scripts:
- Show dotnet ouptut in run script

Installer: 
- Update window title
- Overwrite current musicApp install instead of adding a new entry to list of installed programs
- Download and install .NET 8.0 Desktop Runtime automatically